# Code-Shield Services 核心服务层领域模块化与解耦重构设计

> **重构实施状态**：✅ **已于 2026-09-05 全面圆满实施交付，全部阶段与收尾专项 100% 达成**  
> **实施复盘总结**：详见 [05 - Services 核心服务层模块化架构重构全景实施总结与复盘检视报告](05-服务层模块化架构重构全景实施总结与复盘检视报告.md)

本专题收录了针对 `code-shield/services` 目录的现状全景体检、技术债务诊断报告、领域驱动子包化（Domain-Driven Subpackages）重构设计规范、分阶段平滑落地路线图、以及最终全景落地复盘检视报告。

---

## 📚 文档目录索引与导读

```
services-modular-architecture/
├── README.md                                                        # 本文档：专题导航、重构目标与核心成效
├── 01-Services核心服务层代码现状全景与技术债务诊断报告.md              # 现状量化体检、46个文件职责归类与6大技术债务深度剖析
├── 02-Services领域驱动子包化重构设计与契约规范.md                     # 目标拓扑架构、7大领域子包划分、EngineContext解耦与Facade门面设计
├── 03-分阶段平滑演进路线图与安全落地实施方案.md                       # 4阶段渐进演进计划、落地状态跟踪与回滚防线
├── 04-扫描引擎与执行流水线职责边界与协同模型深度设计.md               # 🌟【核心边界】引擎算法与流水线生命周期正交解耦、6大执行阶段与协同协议
└── 05-服务层模块化架构重构全景实施总结与复盘检视报告.md               # 🏆【落地复盘】全景实施成果、7大提交里程碑、六大维度深度代码检视与最终质量大盘
```

---

## 🎯 重构核心目标与量化指标达成对照

### 1. 解决的核心问题与重构本质
随着 Code-Shield 平台快速演进，近期先后引入了多智能体三权分立对抗辩论引擎（`DebateEngine`）、LLM 算力池平滑加权调度（`ModelDispatcher`）、确定性物理指纹与增量状态机（`DiffEngine`）、Native LLM 直连驱动等多项重大特性。

**重构本质洞察（神似而形散）**：
*   **业务主线（神似）**：现网中 `task_runner.go:RunTaskSync` 已经天然跑在正确的流水线大调度主轴上（Git同步 $\to$ 门禁 $\to$ 引擎 $\to$ 指纹Diff $\to$ 治理Hook $\to$ 脚本后处理 $\to$ 交付），业务灵魂高度成熟；
*   **物理工程（形散）**：由于缺乏及时的模块化组织，原先 **46 个源码文件（16,421 行代码）直接平铺在单个 `package services` 根命名空间中**。重构后成功解决大单包反模式、上帝对象束缚、断点续跑越权、雷同代码冗余与调度层次混杂等全部 5 大结构性痛点。

### 2. 最终量化成效达成对照 (Metrics Scorecard)

| 指标维度 | 重构前现状 (Baseline) | 设计目标指标 (Target) | 最终实际达成 (Actual) | 达标结论 |
| :--- | :--- | :--- | :--- | :---: |
| **`services/` 根目录文件数** | **46 个文件** | $\le 5$ 个文件 | **1 个源码文件 (`services.go`)** | 🌟 **超额达成 (减少 97.8%)** |
| **根目录代码行数** | **16,421 行** | $\le 800$ 行 | **749 行纯轻量门面** | 🌟 **达标 (代码量锐减 95.4%)** |
| **执行引擎直接依赖 DB 读写** | 3 处直接操作 `models.DB` | 0 处直接依赖 | **0 处 (100% 纯内存运算)** | 🌟 **彻底解除状态耦合** |
| **包内循环依赖与跨层访问** | 严重隐式依赖 | 0（强编译期隔离） | **0（ArchGuard 静态门禁守卫）** | 🌟 **编译期单向拓扑成立** |
| **雷同重复代码** | 2 处完全雷同实现 | 0（单一真实源 SSOT） | **0（收敛至 `defects/anchor.go`）** | 🌟 **全系统唯一实现源** |
| **外部引用破坏性** | — | 0 破坏性兼容 | **0 破坏（上层包 0 代码改动）** | 🌟 **100% 平滑透明兼容** |
| **测试与竞态检查通过率** | 100% | 100% | **100% 全部 PASS (`-race`)** | 🌟 **零功能回归交付** |

---

## 🗺️ 最终落地物理架构一览 (100% 对齐设计)

重构后的服务层完全遵循**高内聚、低耦合、领域自治**的原则，形成如下 **9 个领域子包（7 个新建领域子包 + 2 个既有成熟子包）** 与 1 个顶层门面：

*   **双层调度体系**：将职责正交区分为**第一层宏观业务大调度器（`runner`）**（管代码检出、门禁、引擎分发、增量指纹比对、交付）与**第二层微观物理算力池调度器（`dispatcher`）**（管 SWRR 加权、槽位租约、健康熔断）；
*   **契约解耦与纯计算引擎**：用只读、字段最小化的纯数据对象 `EngineContext`（注入负样本规则）与业务产物容器 `EngineResult`（完整承载 Findings、DebateLogs 与 Token 消耗）彻底终结包私有上帝对象 `taskContext`，并将 `context.Context` 提升为 `TaskEngine.Run` 的首参，引擎严禁直接访问 DB，由流水线统筹事务持久化，实现毫秒级纯内存单元测试与零成本插拔扩展。

```
code-shield/services/
├── invoker/             # 【CLI 适配器层】统一 AI CLI 驱动（native, claude, opencode, codex, agy, agent_sync）
├── dispatcher/          # 【算力池调度】LLM 节点权重（SWRR）、槽位租约、健康探活与动态熔断
├── engines/             # 【分析引擎集群】标准化 EngineContext、single、chunked、debate 以及 semantic_chunker
├── defects/             # 【指纹与增量比对】收敛 SourceAnchor（SSOT）、L1物理强指纹、跨任务增量状态机
├── governance/          # 【专项治理与后处理】Campaign 归并引擎、严重度确定性校准树、Taxonomy白名单、误报记忆
├── queue/               # 【任务调度队列】TaskQueue、WorkerPool 竞争拉取与优雅排空
├── reconciliation/      # 【对账与仲裁】（已有独立子包，重构后直接依赖 defects/anchor）
├── reports/             # 【多格式导出】（已有独立子包，Excel/Markdown/JSON 导出）
├── runner/              # 【核心流水线】标准化生命周期 Pipeline、Git同步、JSON清洗、断点续跑、结果通知
└── services.go          # 【兼容门面 Facade】向后兼容导出公共函数，调用方零改动
```

---

## 🚀 快速跳转阅读

1. 📊 **[01 - 代码现状全景与技术债务诊断报告](01-Services核心服务层代码现状全景与技术债务诊断报告.md)**  
   详细了解重构前 46 个文件的完整分布画像、行数统计、以及 6 大技术债务的深度剖析。
2. 📐 **[02 - 领域驱动子包化重构设计与契约规范](02-Services领域驱动子包化重构设计与契约规范.md)**  
   掌握目录拓扑结构、`EngineContext` 传输契约、各子包设计规范与 Facade 门面实现。
3. 🛠️ **[03 - 分阶段平滑演进路线图与安全落地实施方案](03-分阶段平滑演进路线图与安全落地实施方案.md)**  
   查阅从 Phase 1 到 Phase 4 的分步迁移规划、落地状态跟踪及回滚防线。
4. ⚙️ **[04 - 扫描引擎与执行流水线职责边界与协同模型深度设计](04-扫描引擎与执行流水线职责边界与协同模型深度设计.md)**  
   深入理解“算法推演专家”与“生命周期流程管家”的定位区别、6 大标准化流水线阶段、断点续跑归位与基于 EngineContext 的协同协议。
5. 🏆 **[05 - 服务层模块化架构重构全景实施总结与复盘检视报告](05-服务层模块化架构重构全景实施总结与复盘检视报告.md)**  
   全面查阅 7 次提交完整链路、六大维度深度代码检视分析、问题闭环修复与最终全系统验证大盘。
